interface:
  display_name: "Provenance Tracker"
  short_description: "Repo-specific patterns and workflows for provenance-tracker"
  default_prompt: "Use the provenance-tracker repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true