# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
.env
.env.local
*.log
*.err
.vercel

# macOS
.DS_Store

# Obsidian (vault metadata — per-machine, not shared)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# IDE
.vscode/
.idea/

# Local research / consult notes (per-machine, not part of the product)
*_consult.txt

# Disk-backed prose extraction cache (generated at runtime, not shared)
cache/
